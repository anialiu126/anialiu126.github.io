================================================================
NEXT SESSION — context prompt for anialiu126.github.io
(paste this into a new Claude Code session to resume)

claude --resume a464db91-2bf5-42cf-b471-e42e9072f67c
================================================================

WHO / WHAT
- This is Anna Liu's academic website: an al-folio (Jekyll) theme repo at
  /home/ania/Documents/webpage/anialiu126.github.io
- Deploys via GitHub Actions (.github/workflows/deploy.yml) to the gh-pages
  branch on every push to main. Site = https://anialiu126.github.io
- Anna is a gravitational-wave physicist (lensing, ringdown, tests of GR;
  joining Perimeter Institute Sep 2026).

WHAT WAS DONE LAST SESSION (all pushed to main, all deployed)
1. Fixed a YAML syntax error in _data/cv.yml that was BREAKING the deploy:
   an unquoted title contained a colon-space ("Multimessenger Astronomy:
   Bridging...") which YAML read as a mapping. Fix = wrap value in quotes.
2. Rebuilt _bibliography/papers.bib with Anna's real publications:
   - Removed ALL LaTeX (\noop, \textbf, \textcolor, etc.) -- jekyll-scholar
     renders LaTeX LITERALLY, so it was leaking as text on the live page.
   - Kept plain "(N citations)" notes.
   - Collaboration/multi-author papers show "(incl. <em>A. Liu</em>)".
   - Contributions restored ONLY on the 4 LVK-collaboration papers
     (O3b_lensing, LVK_lens_O4a, LVK_TGR_O4a, LIGOScientific GW231123),
     shown via the `note` field.
   - Carried over preview thumbnails + selected flags.
3. Fixed _config.yml scholar name highlight: last_name [Liu], first_name
   [Anna, A.] (was the Einstein template default) -> Anna's name now bolds.
4. Added hyperlinks in cv.yml "Seminar Talks and Conferences": Perimeter
   DOI, LVK webinar YouTube, KIAA Peking, University of Warsaw recording.
5. Freed ~1.6 GB disk (was 100% full): removed ~/.cache/{thumbnails,
   google-chrome,tracker3} and restarted tracker. Now ~2.8 GB free.

OPEN ITEMS / TODO
- [ ] PRETTIER CHECK is still failing (red X). It is HARMLESS -- the
      prettier workflow is separate from deploy.yml and never blocks the
      site. Anna approved fixing it "if local space allows" (option 2:
      install Node, run `npx prettier . --write`, commit, push). Cause =
      trailing whitespace + indentation across many content files
      (cv.yml, *.md, resume.json, _config.yml, etc.). Node is NOT installed
      and disk is tight -- check `df -h /` before installing.
- [ ] VERIFY on the live page that the "(incl. <em>A. Liu</em>)" highlight
      renders italic (not literal <em> tags). Couldn't build locally to
      confirm. If it leaks, switch approach.
- [ ] FUTURE: restyle al-folio to a quieter/editorial aesthetic (Anna liked
      Astro themes QuietPages + Atelier-Ko). Do it IN PLACE via SCSS
      (_sass/_variables.scss, _themes.scss, _base.scss) + layout overrides
      to keep the bibliography engine -- do NOT rebuild on a blog theme.
      Prototype on a separate branch. (Also saved in project memory.)

ENVIRONMENT GOTCHAS
- No `gh` CLI, no `node`/`npm`. Jekyll gems are NOT installed (bundle check
  fails) so local `jekyll build` needs `bundle install` first (heavy).
- Disk lives on one full partition (/). Check space before any install.
- jekyll-scholar config: bibtex_filters [latex, smallcaps, superscript];
  it does NOT execute LaTeX -- use plain text or HTML in bib fields.
  The `note` field and author fields render as RAW HTML (no escaping).
- al-folio bib template = _layouts/bib.liquid. cv renders from _data/cv.yml
  via _includes/cv/*.liquid; items render raw HTML (use <a href> for links).
================================================================
